/**
 * App layouts – single entry for routes. Uses extensions (which can override core).
 */

export { AuthLayout, PublicLayout, AuthLayoutWithSidebar } from '~/extensions/layouts';
export type { AuthLayoutProps, PublicLayoutProps, AuthLayoutWithSidebarProps } from '~/extensions/layouts';
