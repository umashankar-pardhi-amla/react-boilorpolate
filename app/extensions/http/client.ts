/**
 * HTTP Client extension entry point.
 * Re-exports base by default. Replace this file (or its contents) to use a custom
 * ExtendedHttpClient – see client.example.ts for a full override example.
 */

export { httpClient } from '~/core/http';
