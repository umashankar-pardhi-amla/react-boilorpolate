/**
 * Query Config extension entry point.
 * Re-exports base by default. Replace this file (or its contents) to use a custom
 * ExtendedQueryConfig with different defaults (retry, staleTime, etc.).
 */

export { queryConfig } from '~/core/query';
